# Agent Code Intelligence plugin

When this repository is used to develop/test the plugin, use `skills/codebase-intelligence-router/SKILL.md` as the canonical retrieval policy. Prefer exact/structural/local symbol retrieval before semantic or graph tools. Never enable every MCP just because it is listed.
