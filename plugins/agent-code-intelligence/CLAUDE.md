# Agent Code Intelligence plugin

When developing/testing this plugin, use the `codebase-intelligence-router` skill as the canonical retrieval policy. Prefer exact/structural/local symbol retrieval before semantic or graph tools. Never enable every MCP just because it is listed.
